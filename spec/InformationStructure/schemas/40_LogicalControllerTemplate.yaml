profile:
  type: array
  x-key: template-name
  items:
    oneOf:
      - description: 'logicalControllerTemplate'
        type: object
        required:
          - template-name
          - category
        properties:
          template-name:
            type: string
            description: >
              'logicalControllerTemplateName
              candidate: from [CDM://v1/create-logical-controller-template$request.body#logical-controller-template-name]
              running: from candidate
              operational: from running'
          category:
            type: string
            enum:
              - 'logicalController'

          # Please add, remove and change the parameters that need to be managed by template here
